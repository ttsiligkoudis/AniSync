@model AnimeList.Models.LibraryPaneViewModel

@* Shared results-pane partial used by both the initial /library render and
   /library/page's filter-search.js pane-swap. /library renders the user's
   FULL list in one go (no server-side pagination — see LibraryController
   for why), so this partial just delegates to _PosterGrid. *@
@await Html.PartialAsync("_PosterGrid", Model.Grid)
