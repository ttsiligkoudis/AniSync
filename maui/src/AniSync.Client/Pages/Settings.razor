@page "/settings"
@inject NavigationManager Nav

@* The settings surface now lives at /configure (Stremio addon + catalogs + install
   URL), /account (account + linked accounts) and /advanced (sync · backups · danger
   zone) — the same split the web app uses. /settings stays as a stable alias the
   existing nav links point at and redirects to the main configuration page. *@

<PageTitle>Settings · AniSync</PageTitle>

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/configure", replace: true);
}
