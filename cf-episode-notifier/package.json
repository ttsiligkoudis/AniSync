{
  "name": "anisync-episode-notifier",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that cron-triggers AniSync's episode-release notification dispatcher every 5 minutes.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
