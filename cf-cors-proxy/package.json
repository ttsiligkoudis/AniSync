{
  "name": "anisync-cors-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that adds CORS headers to debrid CDN responses so AniSync's embedded-subtitle extractor can stream MKV bytes from the browser.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
